// Character Array
// Count and Print the Length of a String.

#include <stdio.h>
#include <conio.h>

void main()
{
	char x[10];
	int i, j;

	printf("Enter a String: ");
	gets(x);

	for (i = 0; x[i] != '\0'; i++)
	{
	}

	printf("Length of the String is = %d.\n", i);

	getch();
}
